import { MaterialFileInputModule } from './ngx-material-file-input.module';

describe('MaterialFileInputModule', () => {
  it('should work', () => {
    expect(new MaterialFileInputModule()).toBeDefined();
  });
});
